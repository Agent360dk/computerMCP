{
  "name": "computer-mcp",
  "version": "0.2.1",
  "description": "Drive macOS from any MCP client with the guardrails on by default: password fields blacked out in memory before the screenshot is written, a consent gate on every write, and an append-only log that never stores typed text in clear. 28 tools, 12 read-only. No API key, MIT.",
  "mcpServers": {
    "computer": {
      "command": "npx",
      "args": [
        "-y",
        "@agent360/computer-mcp@latest"
      ],
      "env": {
        "CMCP_MODE": "ask"
      }
    }
  }
}
